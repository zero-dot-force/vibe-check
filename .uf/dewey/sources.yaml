# Auto-generated by uf init. Customize as needed.
# This file is user-owned -- uf init will not
# overwrite it after initial creation.

sources:
  # Per-repo disk sources (fine-grained provenance)
  - id: disk-local
    type: disk
    name: vibe-check
    config:
      path: "."

  - id: disk-.github
    type: disk
    name: .github
    config:
      path: "../.github"

  - id: disk-snake-eyes
    type: disk
    name: snake-eyes
    config:
      path: "../snake-eyes"

  # Org-level files (design papers, plans)
  - id: disk-org
    type: disk
    name: org-workspace
    config:
      path: "../"
      recursive: false

  # GitHub API (issues, PRs, READMEs)
  - id: github-zero-dot-force
    type: github
    name: zero-dot-force org
    config:
      org: zero-dot-force
      repos:
        - vibe-check
        - .github
        - snake-eyes
    refresh_interval: daily
